# Artefact créé automatiquement par un plugin MCP en arrière-plan (RAG/mémoire vectorielle) — sans rapport avec le code.
ruvector.db

# État runtime de Claude Code (verrous de tâches planifiées, etc.)
.claude/

# Éditeurs / IDE
.idea/
*.iml

# Dossiers de travail : partitions personnelles (souvent protégées par le droit d'auteur) — ne pas committer leur contenu.
/partitions-sources/*
/partitions-convertis/*
!/partitions-sources/.gitkeep
!/partitions-convertis/.gitkeep

# .NET / C# (build local, jamais committé)
bin/
obj/
*.user
.vs/
